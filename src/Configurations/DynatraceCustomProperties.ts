/**
 * INFO: Custom Properties needs to be created on DT portal first.key specied in the DT portal should match with the value here. Here versionnumber is the key added in DT portal.
 * Key should always be in small case
 */

export const DT_CUSTOM_PROPERTIES = {
  VERSION_NUMBER: 'versionnumber'
}
